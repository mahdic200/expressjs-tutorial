# ExpressJS tutorial sample

## prerequisites

- node >= 16 LTS
- typescript >= 4.5
- mysql with a database

## initializations

```bash
npm install
```

then activate the `.bashrc` file (for linux users or gitbash) :

```bash
. .bashrc
```

then migrate :

```bash
sequelize init:config
sequelize db:migrate
```
